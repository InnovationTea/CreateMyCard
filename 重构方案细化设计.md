下面给一版收敛后的完整方案。核心原则是：**主 Agent 做轻量 SOP 和候选选择，微服务做真实能力裁决、最终 CardSpec、DSL 生成与校验**。主 Agent 不承诺最终一定能生成某类动态卡片，等微服务返回后再组织用户回复。

**一、端到端流程**
```text
App 中控
  -> ROM/App 版本判断，ROM < 7.0 不加载本 Skill

主 Agent
  -> 识别卡片创建场景
  -> 加载 Skill
  -> 调微服务获取能力概述
  -> 选择候选能力
  -> 按需渐进加载数据能力 schema
  -> 构造候选 dataBindings / slots / size
  -> 调微服务生成卡片

微服务
  -> 查 IDS 获取用户安装应用、版本、意图列表
  -> 过滤用户实际可用能力
  -> 根据小艺 App 版本 + ROM 确定 A2UI 协议
  -> 生成最终 CardSpec
  -> 构造 A2UI 模型输入
  -> 生成 DSL
  -> 校验，失败最多重试 1 次
  -> 产物上传 OBS
  -> 返回链接、状态、用户话术

端侧
  -> 解析链接
  -> 下载 artifact
  -> 渲染 DSL
  -> 持久化 DSL + CardSpec
  -> 运行时按 CardSpec 刷新数据
```

**二、Skill 改造目标**

当前 Skill 中很多细节可以下沉：

- 下沉到微服务：A2UI 协议、组件限制、美观规则、素材合法性、DSL 校验、重试修复、最终 CardSpec。
- 保留在 Skill：SOP 流程、能力概述筛选、候选 dataBindings 生成、工具调用、用户回复组织。
- Skill 不再直接输出 `genui/cardspec`，而是输出微服务返回的 `genWidgetResult` 链接和自然语言说明。

**三、主 Agent 调微服务接口**

建议拆 3 个工具。

1. `getWidgetCapabilityOverview`

输入：

```json
{
  "locale": "zh-CN",
  "appVersion": "x.x.x",
  "romVersion": "7.0.0"
}
```

输出：

```json
{
  "dataCapabilities": [
    {
      "id": "ViewWeather",
      "name": "天气",
      "summary": "查询当前天气、空气质量和未来预报",
      "tags": ["weather", "location"]
    }
  ],
  "eventCapabilities": [
    {
      "id": "clickToDeeplink.weather",
      "functionCall": "clickToDeeplink",
      "summary": "打开天气应用"
    }
  ],
  "assetCandidates": [
    {
      "id": "asset.weather.rain",
      "src": "resources/base/media/icon_weather1.png",
      "description": "天气雨伞图标"
    }
  ]
}
```

2. `getDataCapabilitySchemas`

只针对数据能力做渐进加载。

```json
{
  "dataCapabilityIds": ["ViewWeather", "calendar.events.search"]
}
```

返回完整 `inputSchema/outputSchema/defaultWriteResultTo/dataModelSkeleton`。

3. `generateWidgetCard`

主生成接口。

```json
{
  "requestId": "uuid",
  "userQuery": "帮我做通勤卡片，包含天气和今日日程",
  "size": "2x4",
  "candidateDataBindings": [
    {
      "capabilityId": "ViewWeather",
      "arguments": {
        "districtName": "青浦区",
        "forecastDays": 1
      },
      "writeResultTo": "/data/weather",
      "required": false
    },
    {
      "capabilityId": "calendar.events.search",
      "arguments": {
        "timeRange": "today"
      },
      "writeResultTo": "/data/calendar",
      "required": false
    }
  ],
  "candidateEventCapabilityIds": ["clickToDeeplink.weather", "clickToIntent.ViewCalendarEvent"],
  "candidateAssetIds": ["asset.weather.rain", "asset.calendar.schedule"],
  "slots": {
    "districtName": "青浦区",
    "timeRange": "today"
  },
  "options": {
    "allowDegradation": true,
    "returnMode": "obs_url"
  }
}
```

App 版本、ROM、device、uid 等由工具层自动注入，不建议让主 Agent 自己拼。

**四、微服务核心模块**

微服务从零构建，建议只保留必要模块：

```text
WidgetGenerationService
├─ CapabilityRegistry        能力注册表
├─ A2UIProtocolRegistry      A2UI 协议版本表
├─ DeviceCapabilityResolver  IDS 查询与能力过滤
├─ CardSpecBuilder           最终 CardSpec 生成
├─ PromptBuilder             A2UI 模型输入构造
├─ A2UIModelClient           DSL 模型调用
├─ Validator                 DSL/CardSpec/产物校验
├─ ArtifactStore             文件生成与 OBS 上传
└─ ResponsePlanner           降级/拒答/用户话术
```

不要一开始做复杂工作流引擎，普通服务编排即可。

**五、能力过滤设计**

微服务维护能力注册表：

```json
{
  "id": "calendar.events.search",
  "type": "data",
  "inputSchema": {},
  "outputSchema": {},
  "defaultWriteResultTo": "/data/calendar",
  "dependencies": {
    "minRomVersion": "7.0.0",
    "minAppVersion": "x.x.x",
    "requiredPackages": [
      {
        "packageName": "com.huawei.hmos.calendar",
        "minVersion": "14.0.0"
      }
    ],
    "requiredIntentProviders": ["calendar.events.search"],
    "requiredPermissions": ["calendar.read"]
  },
  "dataModelSkeleton": {
    "data": {
      "calendar": {
        "items": []
      }
    }
  }
}
```

过滤顺序：

```text
能力 ID 是否注册
 -> ROM/App 版本是否满足
 -> 依赖 App 是否安装且版本满足
 -> IDS 返回的意图/provider 是否存在
 -> 权限是否 granted/ask_runtime/denied
 -> arguments 是否符合 inputSchema
 -> writeResultTo 是否合法且不冲突
```

注意：App 安装只是必要条件，不是充分条件。最终要以 IDS 返回的意图列表/provider 列表为准。

**六、CardSpec 如何生成**

主 Agent 传的是候选 `candidateDataBindings`。微服务过滤后得到 `effectiveDataBindings`，再生成最终 CardSpec。

```json
{
  "suggestSize": "2x4",
  "dataBindings": [
    {
      "capabilityId": "ViewWeather",
      "arguments": {
        "districtName": "青浦区",
        "forecastDays": 1
      },
      "writeResultTo": "/data/weather"
    }
  ]
}
```

微服务还要做参数规范化，例如把：

```json
{"timeRange": "today"}
```

转换为真实能力需要的：

```json
{"timeInterval": [startMs, endMs]}
```

如果没有有效数据能力，则静态 CardSpec 为：

```json
{
  "suggestSize": "2x2"
}
```

点击事件不进入 CardSpec，只进入 A2UI 模型的候选事件输入。

**七、A2UI 协议版本兼容**

微服务根据小艺 App 版本 + ROM 选择协议：

```text
romVersion + appVersion
 -> protocolProfileId
 -> 组件白名单、属性白名单、尺寸、绑定策略、事件策略、素材策略
```

建议维护：

```json
{
  "profileId": "a2ui-form-rom7-v1",
  "dslVersion": "v0.9",
  "sizeMap": {
    "2x2": {"width": 140, "height": 140},
    "2x4": {"width": 300, "height": 140}
  },
  "allowedComponents": ["Text", "Image", "Divider", "Progress", "Button", "Checkbox", "Row", "Column", "List", "Stack"],
  "bindingPolicy": "path-first"
}
```

生产建议统一 `path-first`，即优先 `{ "path": "/..." }` 和 `formatString`，更适合端侧运行时刷新。

**八、A2UI 模型输入构造**

微服务构造模型输入，不让主 Agent 直接拼 prompt。

输入内容包括：

- 用户原始 query
- 目标尺寸
- 最终 CardSpec 对应的 DataModel 骨架
- 有效事件候选
- 有效素材候选
- A2UI 协议规则
- 美观度规则
- 不可用能力的降级上下文

简化后的 TaskSpec：

```json
{
  "userQuery": "帮我做通勤卡片，包含天气和今日日程",
  "size": "2x4",
  "eventCandidates": [
    {
      "call": "clickToDeeplink",
      "args": {
        "bundleName": "",
        "abilityName": "",
        "uri": "hww://www.huawei.com/totemweather?enterType=share&cityCode="
      }
    }
  ],
  "dataModel": {
    "value": {
      "data": {
        "weather": {
          "location": {},
          "current": {},
          "daily": [],
          "updatedAt": ""
        }
      },
      "state": {
        "loading": true
      }
    }
  },
  "assetCandidates": [
    {
      "assetRef": "weather_icon",
      "src": "resources/base/media/icon_weather1.png",
      "use": "weather icon",
      "description": "天气雨伞图标，适合表达天气信息",
      "static": true
    }
  ]
}
```

**九、校验与重试**

校验对象是完整 artifact 文件，而不是只校验 DSL。

artifact 建议结构：

```json
{
  "schemaVersion": "widget-artifact-v1",
  "requestId": "uuid",
  "genui": "三行 JSONL",
  "cardSpec": {},
  "meta": {
    "protocolProfileId": "a2ui-form-rom7-v1",
    "capabilityRegistryVersion": "2026-07-03",
    "createdAt": 1780000000000
  }
}
```

校验项：

```text
genui 恰好三行 JSONL
createSurface/updateComponents/updateDataModel 顺序正确
root 尺寸与 size/profile 一致
组件、属性、事件、素材都在白名单内
children 引用可解析
updateDataModel.value 与模型输入一致
DSL 绑定路径可从 dataModel 或 CardSpec outputSchema 推导
CardSpec 只包含有效 dataBindings
CardSpec suggestSize 与 DSL 一致
```

失败后最多重新生成 1 次。重试 prompt 只追加错误摘要，不追加大量日志。

**十、降级与拒答**

这是体验关键。主 Agent 看到的是全量能力，微服务才知道真实能力，所以主 Agent 不要提前说“可以生成天气日历卡”。推荐话术是：

```text
我先检查一下当前设备支持情况，然后为你生成可用的卡片。
```

微服务返回统一状态：

```json
{
  "status": "success | degraded | unsupported | failed",
  "artifactUrl": "https://obs.xxx/widget/xxx.json",
  "userMessage": "日历权限未开启，已先生成只包含天气的通勤卡片。",
  "removedCapabilities": [
    {
      "id": "calendar.events.search",
      "reason": "PERMISSION_DENIED",
      "userReadableReason": "日历权限未开启"
    }
  ]
}
```

处理规则：

```text
核心能力可用
 -> 正常生成

部分能力不可用
 -> 降级生成，说明少了什么

组件样式不支持
 -> 不拒答，改写为支持组件表达

数据能力不存在
 -> 不伪造动态 CardSpec，可做静态卡/入口卡

App 未安装
 -> 有入口或静态价值则降级，否则提示安装/启用

所有核心能力不可用且静态卡无价值
 -> 不调用 A2UI 模型，直接 unsupported
```

拒答示例：

```text
当前设备上没有可用的外卖配送数据能力，也没有可打开的应用入口，所以暂时不能生成实时外卖卡片。你可以试试天气、日历或系统状态类卡片。
```

降级示例：

```text
日历权限当前未开启，我先为你生成只包含天气和通勤入口的卡片。开启日历权限后可以再生成包含今日日程的版本。
```

**十一、如何保证卡片好看**

不要靠主 Agent。微服务从三处保证：

- 协议 prompt 内置美观规则：单一主焦点、少区域、受保护文本不截断、按钮贴底、色彩来源合法。
- 场景化素材：通过 `assetCandidates` 给模型可用素材白名单。
- 校验器增加视觉硬规则：区域数量、字号阶梯、间距阶梯、root shell、Row/Column 宽高预算、图片尺寸、禁用网络图。

生成失败或视觉风险高时，微服务强制降级为简单结构：

```text
标题 + 主状态 + 1 条支撑信息 + 1 个入口
```

**十二、OBS 与端侧解析**

微服务上传完整 artifact，不只上传 DSL。

返回：

```json
{
  "status": "success",
  "artifactUrl": "https://obs.xxx/widget/uuid.json",
  "artifactDigest": "sha256:xxx",
  "suggestSize": "2x4",
  "userMessage": "已为你生成通勤卡片。"
}
```

Skill 输出流式标记：

```text
```genWidgetResult:"https://obs.xxx/widget/uuid.json"```
```

端侧下载后拿到 `genui + cardSpec`，用于预览、确认添加和后续刷新。

**十三、开发任务拆分**

第一期最小闭环：

1. App 中控实现 ROM/App 版本分发，ROM < 7.0 不加载 Skill。
2. Skill 改造为工具编排型 SOP。
3. 微服务实现 3 个工具接口。
4. 能力注册表先用配置文件或表维护。
5. 接入 IDS 查询安装应用和意图列表。
6. 实现能力过滤、CardSpecBuilder、TaskSpec/PromptBuilder。
7. 接 A2UI 模型服务。
8. 实现规则校验器和 1 次重试。
9. 上传 OBS，返回 `genWidgetResult`。
10. 打通降级/拒答状态和用户话术。

这版不做复杂模板系统、不做多模型评审、不做在线学习。先保证链路稳定、能力不伪造、卡片能渲染、失败能给用户合理回复。