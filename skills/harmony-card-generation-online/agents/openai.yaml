interface:
  display_name: "Harmony 卡片云侧编排"
  short_description: "判断需求适配与能力满足度，编排云侧插件创建或编辑 HarmonyOS 服务卡片。"
  default_prompt: "使用 $harmony-card-generation-online 创建或编辑 HarmonyOS 服务卡片；在卡片上下文中先判断需求适配和能力满足度，无法满足时提前结束并给出安全的相近场景，部分满足时先说明再自动生成，结果只使用真实状态和 artifactUrl。"
