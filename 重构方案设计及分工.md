# Widget 云侧skill改造开发参考文档

## 1. 文档目的

本文用于指导 Create My Widget / CreateMyCard 云侧改造落地，面向方案、Skill、微服务、联调和测试人员。

目标是在复用现有对话链路的前提下，将原本由主 Agent 完成的大部分卡片生成逻辑下沉到微服务中，主 Agent 只负责轻量 SOP、能力候选选择和用户回复组织；微服务负责用户实际能力过滤、CardSpec 生成、A2UI DSL 生成、校验、降级和产物存储。

## 2. 当前结论

### 2.1 目标

- 用户在 App 中输入自然语言需求，生成可预览、可添加到桌面的 HarmonyOS A2UI Form 卡片。
- ROM 版本低于 7.0 时，不加载本 Skill，不触发卡片生成流程。
- 支持动态卡片：通过 CardSpec 声明端侧数据能力，端侧运行时刷新数据。
- 支持静态/降级卡片：当部分数据能力不可用时，仍尽量生成有价值的卡片。
- 确保生成结果协议合法、布局可渲染、样式质量可接受。

### 2.2 非目标

- 一期不接入三方 App 数据能力，例如外卖、打车实时状态等。
- 一期不做复杂模板系统，不要求模型复制历史模板。
- 一期不做在线学习、自动 A/B 风格优化、多模型投票评审。
- 一期不让主 Agent 直接生成最终 DSL。

## 3. 端到端流程

```text
Step 1: App 中控根据 App 版本和 ROM 版本决定是否加载 Skill
        ROM < 7.0: 不加载本 Skill
        ROM >= 7.0: 允许 Skill Search / Skill 加载

Step 2: 主 Agent 判断用户 query 是否匹配卡片创建场景

Step 3: 主 Agent 调微服务获取能力概述
        返回数据能力概述、事件能力概述、素材概述

Step 4: 主 Agent 根据 query 筛选候选能力
        数据能力按需调用微服务加载完整 schema
        事件能力和素材能力可在 Step 3 直接全量返回

Step 5: 主 Agent 将 query、候选能力、候选 dataBindings、size 等信息传给微服务
        工具层自动携带用户 App 版本、ROM 版本、device、uid 等上下文

Step 6: 微服务生成卡片
        6.1 查询 IDS，获取用户实际安装应用、版本、意图/provider 列表
        6.2 过滤得到用户实际可用能力集合
        6.3 根据小艺 App 版本 + ROM 版本选择 A2UI 协议 profile
        6.4 生成最终 CardSpec，构造 A2UI 模型输入并生成 DSL
        6.5 校验产物；失败最多重新生成 1 次
        6.6 生成 artifact 文件，上传 OBS，返回链接和结构化状态

Step 7: 主 Agent 收到微服务结果，向用户回复
        success/degraded: 返回卡片生成结果标记
        unsupported/failed: 返回用户可理解说明和替代建议

Step 8: 端侧解析文件链接，下载 artifact，渲染 DSL 并持久化 DSL + CardSpec
```

## 4. 系统边界

### 4.1 App 中控

职责：

- 根据 ROM/App 版本决定是否加载本 Skill。
- ROM < 7.0 时，不召回本 Skill。
- 将必要设备上下文放入会话或工具调用上下文。

不负责：

- 不做能力过滤。
- 不做 DSL 生成。

### 4.2 主 Agent / Skill

职责：

- 识别卡片创建场景。
- 调微服务获取能力概述。
- 根据用户 query 选择候选能力。
- 生成候选 dataBindings 或候选能力计划。
- 调微服务生成卡片。
- 根据微服务返回的状态组织自然语言回复。

不负责：

- 不维护完整 A2UI 协议。
- 不直接生成最终 DSL。
- 不承诺用户设备一定支持某项能力。
- 不生成最终 CardSpec。

### 4.3 微服务

职责：

- 维护能力注册表和版本兼容要求。
- 查询 IDS 获取用户实际设备能力。
- 过滤候选能力，得到实际可用能力。
- 生成最终 CardSpec。
- 根据版本选择 A2UI 协议 profile。
- 构造 A2UI 模型 system prompt 和 user messages。
- 调 A2UI 模型生成 DSL。
- 校验 DSL/CardSpec/artifact。
- 处理降级、拒答、失败重试。
- 上传 OBS，返回 artifact 链接和用户可读信息。

### 4.4 A2UI 模型服务

职责：

- 根据微服务构造的输入生成 `genui` DSL。

不负责：

- 不查询用户设备能力。
- 不生成最终 CardSpec。
- 不判断用户是否安装 App。

### 4.5 端侧

职责：

- 解析 `genWidgetResult` 标记。
- 下载 artifact。
- 渲染 DSL。
- 用户确认后持久化 DSL + CardSpec。
- 卡片刷新时按 CardSpec 执行数据能力，向 DSL `updateDataModel` 写入数据。

## 5. Skill 开发参考

Skill SOP：

```text
1. 判断用户是否在请求创建卡片
2. 调 getWidgetCapabilityOverview 获取能力概述
3. 根据 query 选择候选数据能力、事件能力、素材
4. 对选中的数据能力调用 getDataCapabilitySchemas
5. 生成 candidateDataBindings、size、slots
6. 调 generateWidgetCard
7. 根据返回状态回复用户
8. success/degraded 时输出 genWidgetResult 标记
```

Skill 注意事项：

- 不直接输出 genui/cardspec。
- 不将完整 A2UI 规范放进 Skill。
- 不提前承诺设备支持某能力。
- 对微服务返回的 `userMessage` 做轻量润色即可。
- `status=unsupported/failed` 时不要输出 `genWidgetResult`。

输出标记：

````text
```genWidgetResult:"https://obs.xxx/widget/uuid.json"```
````

## 6. 版本兼容

必须记录和传递的版本：

- ROM 版本
- App 版本
- 小艺 App 版本
- capability registry 版本
- A2UI protocol profile 版本
- artifact schema 版本
- DSL protocol 版本

兼容策略：

- ROM < 7.0：App 中控不加载本 Skill。
- ROM >= 7.0：进入本流程。
- 微服务根据 ROM + 小艺版本选择 A2UI profile。
- 不同 profile 可有不同组件白名单、尺寸、属性支持范围。
- 产物 meta 中记录 profile，方便端侧兼容和问题追踪。


## 7. 我的职责

职责：

- 改造 Skill SOP。
- 接入三个微服务工具。
- 实现能力概述筛选 prompt。
- 实现候选 dataBindings 生成逻辑。
- 实现 success/degraded/unsupported/failed 回复策略。

交付物：

- 新版 Skill。
- Skill 测试样例。
- 主 Agent 工具调用样例。
- 用户回复话术样例。


## 8. 待办事项清单
### Skill

- [ ] 新增工具型 SOP。
- [ ] 接入 `getWidgetCapabilityOverview`。
- [ ] 接入 `getDataCapabilitySchemas`。
- [ ] 接入 `generateWidgetCard`。
- [ ] 实现候选能力选择 prompt。
- [ ] 实现候选 dataBindings 生成。
- [ ] 实现用户回复话术。
- [ ] 实现 `genWidgetResult` 输出。
- [ ] 准备 10 条典型 query 回归样例。